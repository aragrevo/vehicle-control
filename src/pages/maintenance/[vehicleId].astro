---
if (!Astro.locals.user) {
  return Astro.redirect("/login");
}

const { vehicleId } = Astro.params;

if (!vehicleId) {
  return Astro.redirect("/maintenance");
}

return Astro.redirect(`/maintenance/new?vehicleId=${vehicleId}`);
---
