---
import Button from '../ui/Button.astro';

interface Props {
  vehicleId: string;
  document?: {
    id: string;
    type: string;
    expiresAt: number | null;
    alertDaysBefore: number | null;
    fileUrl: string | null;
  };
  action: string;
}

const { vehicleId, document, action } = Astro.props;

const documentTypes = [
  { value: 'itv', label: 'ITV' },
  { value: 'insurance', label: 'Seguro' },
  { value: 'registration', label: 'Permiso de circulación' },
];
---

<form method="POST" action={action} class="space-y-md">
  <input type="hidden" name="vehicleId" value={vehicleId} />

  <div>
    <label for="type" class="block text-label-md text-on-surface-variant uppercase tracking-wider mb-xs">
      Tipo de documento *
    </label>
    <select
      id="type"
      name="type"
      required
      class="select-base"
    >
      <option value="">Seleccionar...</option>
      {documentTypes.map((type) => (
        <option
          value={type.value}
          selected={document?.type === type.value}
        >
          {type.label}
        </option>
      ))}
    </select>
  </div>

  <div class="grid gap-md md:grid-cols-2">
    <div>
      <label for="expiresAt" class="block text-label-md text-on-surface-variant uppercase tracking-wider mb-xs">
        Fecha de caducidad
      </label>
      <input
        type="date"
        id="expiresAt"
        name="expiresAt"
        value={document?.expiresAt ? new Date(document.expiresAt).toISOString().split('T')[0] : ''}
        class="input-base"
      />
    </div>

    <div>
      <label for="alertDaysBefore" class="block text-label-md text-on-surface-variant uppercase tracking-wider mb-xs">
        Días de aviso previo
      </label>
      <input
        type="number"
        id="alertDaysBefore"
        name="alertDaysBefore"
        min="1"
        max="365"
        value={document?.alertDaysBefore || 30}
        class="input-base"
      />
    </div>
  </div>

  <div>
    <label for="fileUrl" class="block text-label-md text-on-surface-variant uppercase tracking-wider mb-xs">
      URL del archivo (opcional)
    </label>
    <input
      type="url"
      id="fileUrl"
      name="fileUrl"
      value={document?.fileUrl || ''}
      class="input-base"
      placeholder="https://..."
    />
  </div>

  <div class="flex items-center gap-md pt-md border-t border-outline-variant">
    <Button type="submit" variant="primary">
      {document ? 'Guardar cambios' : 'Añadir documento'}
    </Button>
    <Button href={`/documents`} variant="ghost">
      Cancelar
    </Button>
  </div>
</form>
